# 2025-12-12 学習ログ

## やったこと
- WSL2 + Ubuntu インストール
- AWS CLI インストール
- IAMユーザー `tomo-study` 作成（AdministratorAccess 付与）
- アクセスキー発行 → `aws configure`
- `aws sts get-caller-identity` 成功（認証OK）

## 成果（できたこと）
- WSL2上からAWSに認証できる状態になった

## メモ
- 使った環境：Windows 11 + WSL2(Ubuntu)
- Region：ap-northeast-1（東京）

## 実行コマンド（要点）
```bash
sudo apt update
aws --version
aws configure
aws sts get-caller-identity
```

## 実行コマンド aws sts get-caller-identity の実行結果(IDとアカウント名は伏せている)
```json
{
  "UserId": "...",
  "Account": "...",
  "Arn": "arn:aws:iam::<ACCOUNT_ID>:user/tomo-study"
}
```